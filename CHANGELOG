CHANGELOG

v1.0.2
- No changes (figuring out how to merge changes from beta branch to latest release)

v1.0.3
- Make quick button label and value fields more distinct
- Improve tooltips on mobile / touch devices
- Adjust quick button spacing to be a bit more compact
- (Bug) Add $ symbol to auto-label when amount is $999 or less

v1.0.4
- (Feature) Add toggleable fields to buy/sell a stock by dollar amount
- (QOL) Add tooltips to allow buy / allow sell toggles
- (QOL) Add tooltip to create new quick button
- (QOL) Add tooltips to block all buy / block all sell
- (QOL) Improve tooltip so it doesn't overflow edges of screen on smaller viewports
- (Bug) Adjust purchase amount calculation logic, account for 0.1% selling fee.